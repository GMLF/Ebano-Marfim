# Copie este arquivo para ".env" e preencha com os valores reais
# (Supabase > Settings > API). O ".env" nunca é commitado — este
# arquivo de exemplo é o único que fica versionado.

SUPABASE_URL=https://SEU-PROJETO.supabase.co
SUPABASE_ANON_KEY=SUA-PUBLISHABLE-KEY-AQUI
